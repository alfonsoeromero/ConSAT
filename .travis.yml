language: python
python:
  - "3.6"      # current default Python on Travis CI
install:
  - pip install -r requirements.txt
script:
  # python tests
  - python -m coverage run --branch --source gfam -m unittest discover -s test -v
  # coverage reports
  - python -m coverage report -m
